# change these values as you see fit
# after running `sudo ./install`, the server should run at http://host:port/base_url
host = '0.0.0.0' # for public request
port = 8000
base_url = 'index/'
username = 'admin'
password = 'mypassword'

# for example, with above default values, the server should run at 127.0.0.1:5000/index/
